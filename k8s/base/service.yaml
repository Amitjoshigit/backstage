apiVersion: v1
kind: Service
metadata:
  name: backstage-app-service
  namespace: backstage
  labels:
    app: backstage-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: backstage-app
    version: v1
